import {
  increment
} from 'ping-pong-client/helpers/increment';

module('IncrementHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = increment(42);
  ok(result);
});
