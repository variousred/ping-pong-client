import {
  incrementInteger
} from 'ping-pong-client/helpers/increment-integer';

module('IncrementIntegerHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = incrementInteger(42);
  ok(result);
});
